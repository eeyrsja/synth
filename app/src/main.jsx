import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import GraphingCalculatorSynthApp from "./graphing_calculator_synth_app.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GraphingCalculatorSynthApp />
  </React.StrictMode>
);
